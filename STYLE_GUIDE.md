# 王祥辉作品集样式规范

## 1. 命名

- 整站统一称为“王祥辉｜作品集”。
- 首页四个入口区域统一称为“作品目录”。
- “个人简历、关于我、AI 应用、联系我”是四个章节名，不再使用“作品档案”作为并列名称。
- 面向用户的导航、章节标记和辅助说明以中文为主；英文只保留在书封分类等装饰性位置。

## 2. 字体职责

- `Instrument Serif`：品牌名、首页主标题、页面一级标题、章节标题和卡片标题。
- `Newsreader`：导航、正文、说明文字、按钮、标签和数字信息。
- 标题使用 600 左右字重，正文使用 400；不在同一级标题中混用多个字重。

## 3. 字号层级

| 层级 | 桌面端 | 移动端 |
| --- | --- | --- |
| 顶部品牌 | 22px | 20px |
| 导航与返回 | 16px | 14px |
| 首页主标题 | 66–108px | 46–58px，两行固定节奏 |
| 详情页主标题 | 68–140px，长标题按宽度收缩 | 52–68px，长标题自然换行 |
| 章节标题 | 36–58px | 34–42px |
| 正文 | 18–23px | 17px |
| 卡片标题 | 24–30px | 24px |
| 卡片正文 | 17–19px | 16px |
| 标签与元信息 | 12–17px | 11–12px |

同一层级通过 CSS 变量控制。长标题只允许为了避免换行或溢出而缩小，不单独放大。

## 4. 布局与间距

- 桌面正文最大宽度为 1120px；首页主文案最大宽度为 1080px。
- 详情页纸张容器桌面宽度不超过 1120px，移动端保留 9px 外边距。
- 主要间距遵循 8px 基础节奏，常用值为 8、16、24、32、40、48、60px。
- 正文行高为 1.75–1.82；卡片正文行高为 1.7。
- 长段落占满内容区并两端对齐，最后一行左对齐；手机端允许自然分段，不压缩字号来强行减少行数。

## 5. 形状与控件

- 顶部导航使用全宽直角通栏；不再给品牌单独套胶囊。
- 胶囊只用于返回按钮、状态标签和关键词标签。
- 内容卡片圆角统一在 18–20px；详情页主纸张圆角为 24–42px。
- 桌面可点击控件高度至少 40px，移动端至少 44px；纯文字导航保留足够的上下点击区域。

## 6. 配色

- 首页前两屏保留动态森林；首页作品目录与四个详情页使用统一的纯色森林灰绿，避免动态背景干扰长内容阅读。
- 正文纸张使用低饱和浅绿灰，不使用纯白。
- 强调色仅使用苔藓绿与浅黄绿；红棕色只用于“联系我”书封等有限分类提示。
- 正文、按钮和卡片优先保证对比度。移动端详情页导语保留轻量深绿底，使其与纯色背景保持清晰层级。

## 7. 响应式检查

- 主要验收宽度：1440px 桌面，以及 360px、390px、430px 手机。
- 页面不得产生横向滚动；标题、邮箱和返回按钮不得被裁切。
- 手机端顶部导航采用品牌与章节两行结构，高度 98px；桌面端为 58px。
- 手机端首屏之后直接进入四个触控章节，不加载桌面端 3D 翻书；每个章节入口至少 142px 高。
- 手机端动态森林延迟到首屏内容可操作后加载，使用约 1 万株苔藓、20fps 和 1 倍像素比，并跳过桌面端线框扫描；桌面端使用约 5.2 万株苔藓和 45fps。
- 动态背景可以继续交互，但不能压住正文和主要按钮。
